form := atom | list

list := '\(' form '\)'

atom := ignored | doubleQuoted | comment | other

// tokens
ignored := '[\s,]*'
doubleQuoted := '"(?:\\.|[^\\"])*"?'
comment := ';.*'
other := '[^\s\[\]{}(\'"`,;)]*'
